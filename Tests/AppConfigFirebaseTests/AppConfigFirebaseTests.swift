import XCTest
@testable import AppConfigFirebase

final class AppConfigFirebaseTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AppConfigFirebase().text, "Hello, World!")
    }
}
